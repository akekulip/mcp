# P3 feedback audit — sensitivity only; end-to-end result not yet measured

**Status:** corrected 2026-08-28. The earlier version of this file overclaimed P3. It compared a
full controller sweep with a 97.4 us same-switch congestion-attention reaction and called the
latter C-W4 feedback. Those are different mechanisms. The old fast-path conclusion and deterministic
flapping table are retracted.

## What is actually known

A C-W4 discontinuity is observed downstream, while P2's health gate changes the spray choice at the
source. The required path is therefore:

```text
downstream C-W4 gap -> attributed event transport -> source selector -> exact health-gate update
```

That complete path has not been measured or built. Existing silicon results provide two controller
references, neither of which is the P3 result:

- **2.20 ms:** minimal one-slot register read plus write;
- **106.6 ms:** median full-sweep Python controller loop.

The **97.4 us** result in `p4/reports/h7-timing-F6.md` is an in-pipeline reaction to locally visible
congestion. It does not transport a downstream loss event upstream. The repository's loss-specific
F1 report instead records a 10.115 ms host observation path and explicitly says no in-switch loss
path was built.

The exact latency and write cost of an attributed C-W4 event updating `tbl_health_gate` remain
unmeasured.

## Corrected sensitivity

The table below retains the old analytical assumptions—25 Gbit/s, 1500-byte packets, one context at
25% of link traffic, and the sequential-test detection approximation—but labels the two nonzero
latencies as references. It is generated by `python3 sim/sublink/feedback.py`.

| fault rate | approximate detection | unsafe, instant lower bound | unsafe, 2.20 ms reference | unsafe, 106.6 ms reference |
|---|---:|---:|---:|---:|
| 1e-2 | 0.27 ms | 1.4 | 12.9 | 556.6 |
| 1e-3 | 1.92 ms | 1.0 | 2.1 | 56.5 |
| 1e-4 | 19.20 ms | 1.0 | 1.1 | 6.6 |
| 1e-5 | 192.00 ms | 1.0 | 1.0 | 1.6 |

This supports only a sensitivity statement: feedback delay matters when it is comparable to the
time needed to observe enough loss. It does **not** select a data-plane design. At p=1e-2, 97.7% of
the difference between the two controller-reference exposures comes from comparing a minimal
attributed operation with a full sweep, so the controller's event/update scope must be measured
before deciding that an in-data-plane return path is necessary.

## Controller state that now exists

`controller/sublink_feedback.py` contains a testable decision core and a narrow BFRT writer:

- one downstream event names `(directed vlink, context, epoch)`;
- the frozen pooled inference layer decides quarantine;
- one link-context decision expands into all exact P2 keys sharing that directed link;
- duplicate decisions coalesce per sublink epoch;
- earlier-epoch events are rejected;
- restoration requires positive probation/audit observations—silence after rerouting is not
  treated as evidence of health;
- BFRT add/modify/delete uses the real P2 fields and does not mask non-BFRT programming failures.

This is not an event source or transport. No running process currently constructs `GapEvent` from
the data plane, and no probation/audit path currently produces restoration evidence.

## Structural observability limit

Ordinary C-W4 exposes a loss only when a later packet in the **same context sequence** survives and
arrives with a discontinuity. A context that becomes a complete black hole has no later survivor,
so it produces no C-W4 gap. This directly affects the motivating “all packets above 1 KB disappear”
case. P4 must not replay that case as though P3 had detected it.

P3 can pass only after one of these claim boundaries is chosen and implemented:

1. restrict the detector claim to partial conditional loss with same-context survivors, and price a
   separate liveness/audit mechanism for total black holes; or
2. add a cross-context/liveness witness and prove its encoding, wrap/reorder behavior, false alarms,
   compiler fit, and residual blind spots.

## Remaining P3 gate

Before P4 can support an end-to-end paper claim, the repository needs all of the following:

- a real attributed event source and downstream-to-source transport;
- a batch-safe health-gate update path with measured switch/controller latency;
- an explicit probation or audit route for restoration;
- replay of persistent, repaired, flapping, reordering, wrap, partial-loss, selective-blackhole,
  and all-context-blackhole cases through the implemented epoch state machine;
- matched false-alarm/safety operating points with confidence intervals.

Until then, P3 is **PARTIAL** and this file is a sensitivity record, not a result.

# Novelty "kill-list" matrix — data-plane-native measurement controller for packet-sprayed RDMA/AI fabrics

Generated 2026-08-25. Status updated 2026-08-28. Zotero collection:
`MCP-sprayed-fabrics` (key `AT2STS8I`, 39 items).

> **Status:** this matrix tested the original attention/bandit thesis. That thesis is retired as a
> headline contribution after both blocking novelty gates failed in
> `docs/review/NOVELTY-GATE.md`. The rows and rebuttals below are retained as the historical
> kill-list, not as current absence claims. The replacement counterfactual-observability thesis is
> provisional until its separate primary-source gate passes.

## Retired thesis (historical matrix target)

Switch keeps per-(dst-leaf, spine) uncertainty/anomaly state and an **attention weight per path** in SALU registers; the weight **gates mirroring / INT / CSIG-style tagging** toward the paths that are least certain or most deviant. The **NIC's per-path evidence** (RTT / ECN / PSN-gap) is **reflected in-band** to the switch. A control-plane **constrained bandit with shadow prices** sets per-resource budgets (mirror bandwidth, collector CPU, SRAM, INT bytes) **per collective iteration**.
Claims: (C1) faster gray-failure localization at equal budget than uniform sampling, SprayCheck round-robin and OmniPath-style probing; (C2) works on lossy and 3-level fabrics; (C3) NIC+switch loop beats either alone.

## Evidence legend

- **F** = full text (or the substantive sections) read this session; section/page cites are real.
- **A** = abstract / structured summary only (ACM DL and IEEE Xplore returned 403/empty to the fetcher).
- **K** = prior knowledge, unverified this session — treat as a hypothesis to check before citing numbers.

Column key: *Where decided* = which element makes the measurement/adaptation decision (controller / switch / NIC / fixed). *Spray* = designed for per-packet spraying or adaptive routing (Y/N/partial). *Topo/loss* = evaluated topology depth and lossless (PFC) vs lossy assumption.

---

## Part A — the assigned list

| # | Paper (venue/yr, ev.) | What is measured | Where decided | Signal driving adaptation | Resources budgeted / priced | Learning? | Spray | Hardware | Topo / loss | Headline result | Reviewer: "you are incremental over this because…" | Our rebuttal |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 1 | **OmniPath Ping** (SIGCOMM'26, DOI 10.1145/3789240.3829105; **A** — the provided PDF URL 404s, ACM 403) | Per-hop telemetry of *probe* packets under spraying; service-flow connectivity/perf ("service tracing") | Host issues probes; switch does in-network "OmniPath cache", duplication + dedup; **fixed coverage policy**, not adaptive | None — objective is *full path coverage with zero redundancy*; "topology-aware concurrency control" trades cache SRAM vs probe timeliness | Switch SRAM (181 KB) and probe timeliness; no mirror/INT/collector budget | No | **Y** (first primitive for spraying) | Programmable switch + host (vendor not in abstract; K: Tofino) | Not stated in abstract | 10 ms-level service tracing, full path coverage, 181 KB SRAM | "OPP already resolves path ambiguity in-network and covers every path under spraying; your attention weights are just a probe-scheduling heuristic on top of an existing primitive." | OPP is **coverage-maximising and uniform**: it spends the same on every path every round and measures *probes*, not the live sprayed data. We (i) measure the collective's own packets, (ii) concentrate a *fixed* budget on uncertain/deviant paths, (iii) fuse NIC evidence. OPP is an *arm* our bandit can pull, and the natural "OmniPath-style probing" baseline for C1. |
| 2 | **SprayCheck** (arXiv 2605.03702, May 2026; **F** via arXiv HTML) | Per-spine packet counts of one prioritised cross-leaf flow at the **destination leaf**; one-sided Z-test threshold t = λ − s·√(N/k) (§3.5) | Data plane counts + compares; **control plane computes threshold**; source leaf picks flows by **coordination-free local round-robin** (§3.4); central monitor localises by path intersection (§3.6) | Count imbalance across spines (loss only); no online adaptation of *what* to measure | < 2 KB SRAM per 32 spines (§4.2); one flow prioritised (0.25 % slowdown to others, §5.6). No mirror/INT/collector budgeting | Empirical calibration of sensitivity s on ROC curves (§5.3); no online learning | **Y** | 2× Tofino-1 (one split into 16 virtual switches), ConnectX-6 DX, DCQCN, RNIC-SR; NS-3 + Astra-sim JSQ(2) (§5.1, App. B) | **2-level only**; **lossless assumed** — "every packet drop … is a network failure" (§2); §6 lists lossy fabrics, 3-level, weighted spraying, access links as future work | 1.5 % per-link drop detected in 1 iteration; 0.5 % in 5 iterations of Llama-3-70B, 64 spines (Table 1) | "SprayCheck already localises gray failures inside Tofino under adaptive routing with 2 KB of state and no coordination; re-ordering its round-robin by an 'attention weight' is a tuning knob, not a contribution." | SprayCheck's own §6 is our gap list: **loss-only** signal, **lossless-only**, **2-level only**, **round-robin coverage with no notion of budget**. We add multi-signal evidence (RTT/ECN/PSN-gap) so we work when losses are *expected* (lossy UET/MetaRoCE fabrics), a per-path posterior so budget follows uncertainty rather than the calendar, and switch-to-switch composition for 3-level. SprayCheck's per-spine counter is one feature in our state and its round-robin is exactly baseline B2 in C1. |
| 3 | **FlowPulse** (HotNets'25, DOI 10.1145/3772356.3772384; **F**, pp. 1–7) | Per-ingress-port (spine→leaf) data volume per collective iteration vs a predicted "temporal symmetry" load model (§4, §5.1) | **Leaf switch** autonomously (counter vs prediction); prediction computed in switch control plane from analytical / simulated / learned model (§5.2) | Deviation > 1 % from predicted volume (§5.3); localisation by comparing senders (§5.3, Fig. 4) | None — measures one prioritised collective; no actuation of telemetry | "Learning" = take first-iteration load as baseline and re-baseline after transient recovery (§5.2, Fig. 3) | **Y** | **NS-3 only** (§6) | 2-level non-blocking, 32 leaf × 16 spine, **lossless**, no CC (§6); 3-level and blocking left to future work (§7) | 1 % threshold is a perfect classifier for ≥ 1.5 % drop; radix 32 misses 0.8 % (§6, Fig. 5) | "Per-iteration, coordination-free, in-switch deviation detection for sprayed collectives is FlowPulse." | FlowPulse *detects*; it neither localises beyond adjacent links nor decides **where to spend telemetry**; sim-only; symmetric-fabric assumption. Its deviation statistic is one anomaly input to our attention weight. |
| 4 | **MetaRoCE** (Meta Engineering blog, 2026-08-24; **A**/blog) | NIC-side per-logical-path RTT, ECN state, utilisation; per-path SACK bitvectors → per-path loss | **NIC** (path selection, spraying, recovery); switches need only ECN + ECMP | Per-path health ("tell congestion from failure and rebalance explicitly") | None (transport) | No | **Y** (NIC-side spraying by UDP src-port entropy) | 64-node AMD GPU cluster; commodity switches | Fat-tree / multiplane; **lossy, no PFC** | ~86 % throughput at 1 % loss; still functional at 10 % loss | "The NIC already has per-path RTT/ECN/loss and moves traffic off bad paths in real time; switch-side attention state is redundant." | The NIC sees only *its* paths from *its* vantage, cannot attribute a bad path to leaf-link vs spine, and — by routing around — **hides** the failure from operators. Our loop reflects that NIC evidence in-band so the switch can aggregate across sources and localise. MetaRoCE explicitly wants *no* INT; we quantify the marginal value of adding budgeted switch attention on top of exactly this NIC (C3). |
| 5 | **CSIG** (SIGCOMM'26 DOI 10.1145/3789240.3829179, **A**; IETF draft-ravi-ippm-csig-01, expired; Google LPC-2025 slides **F**) | Bottleneck signal along path: min available bandwidth, min ABW/C, max per-hop delay; **4-byte L2 tag** = 16 b TPID, 3 b type, 5 b bucketed value (32 buckets), 7 b locator metadata (LPC slide "L2 tag format"); 8 B expanded tag exists | Switch **compare-and-replace** per hop; receiver reflects "latest seen" value in an L4 option; sender CC decides | Fixed, every tagged packet; no adaptation | None — fixed per-packet overhead by design | No | Per-packet tags are inherently per-path, but reflection is "latest seen … not 1-to-1" (LPC slide), so path identity is lost at reflection | > a dozen switch and NIC ASICs; deployed at scale in Google DCs; UEC standardised v0.5 | Multi-tier; TCP/lossy | Paper abstract: −20 % median RPC latency, −60 % unclaimed bandwidth in production | "CSIG already delivers a multi-bit bottleneck signal plus a bottleneck locator in 4 bytes on every packet; you merely turn it on and off." | CSIG is an always-on, uniform *congestion* signal for CC. We treat CSIG tagging as one **budgeted actuator** applied selectively to uncertain paths, use its locator bits as evidence, and add what CSIG lacks (loss / PSN-gap, per-path identity preserved through reflection). |
| 6 | **CPRANT** (IEEE TNSM early access 2026, DOI 10.1109/TNSM.2026.3722680; **title only** — abstract unavailable from Xplore, Semantic Scholar and Crossref) | From title: SRv6-based telemetry architecture for computing-power networks with FPGA optimisation | Unknown (likely SRv6 source + FPGA collector) | Unknown | Unknown | Unknown | N (SRv6 source routing, not spraying) | FPGA | WAN / CPN | Unknown | "FPGA-accelerated SRv6 telemetry collection already exists." | Different domain (WAN computing-power networks, source-routed). **Re-check when full text is accessible**; do not cite specifics until then. |
| 7 | **DynATOS** (NSDI'22; **A** + project page) | Approximate traffic queries (heavy hitters etc.) with error bounds | **Controller scheduler**; time-division approximation, sub-epoch multiplexing of switch resources via an optimisation | Query accuracy bounds and traffic/query dynamics | Single switch ASIC stages + memory among concurrent queries | No (optimisation formulation) | N | Broadcom runtime-programmable switch | n/a (single switch) | High query satisfaction at 1 query/s arrival while preserving accuracy | "Dynamically budgeting scarce switch resources among telemetry tasks under dynamics is DynATOS." | DynATOS multiplexes **one switch's compute among queries**; no path objective, no NIC loop, no cross-device pricing of mirror bandwidth / collector CPU / INT bytes, no failure localisation. |
| 8 | **DynATOS+** (IEEE/ACM ToN'24, DOI 10.1109/TNET.2024.3367432; **A**) | As above, for dynamic traffic *and* query workloads | Controller | Same | Same | No | N | Same | n/a | Extends robustness to traffic dynamics | Same as 7 | Same as 7. |
| 9 | **CoordSamp** — arXiv title "Coordinated Sampling in SDNs with Dynamic Flow Rates" (arXiv 2409.05966; ToN'26; **F** via arXiv HTML) | Which switch samples which flow so flows reach target sampling rates despite rate fluctuation (§III-A) | **Central SDN controller**, per epoch; ISOCP → ILP / APX approximation (§III) | Historical mean/variance of flow rates (§II) | **Per-switch sampling capacity (pps)** with chance-constraint violation probability δ (§III-A) | No (stochastic programme) | N | ns-3, OpenFlow, Abilene (11 nodes) (§IV-A) | WAN; n/a | +10 % fully sampled flows; APX 0.005 s vs ILP 268 s (§IV-B) | "Budgeted, uncertainty-aware network-wide sampling allocation is an existing chance-constrained programme." | Flow-level, controller-epoch, single-path SDN; no in-data-plane adaptation, no paths, no failure objective. Its chance-constraint formulation is a legitimate control-plane **baseline** for our bandit, not a substitute. |
| 10 | **BRIGHT** (ICACT 2026, Gong/Chen/Zhu, DOI 10.23919/ICACT68090.2026.11431419; **A**) | Per-flow rates in OpenFlow SDN; **Gaussian posteriors of per-flow rate and their uncertainty** | Controller; Bayesian RL with spatio-temporal scheduling of polling | Posterior uncertainty vs monitoring cost | Monitoring (polling) cost vs estimation accuracy | **Yes — Bayesian RL** | N | OpenFlow testbed | n/a | Improves cost/accuracy trade-off over baselines (numbers not in abstract) | "Uncertainty-directed measurement with Bayesian posteriors and RL scheduling is BRIGHT." | Conceptually the closest on *uncertainty-directed budgeting*, but it is controller-polled OpenFlow flow-rate estimation — no data-plane state, no paths/spraying, no failure objective, no NIC. We must cite it and position our per-path posterior in SALUs as the data-plane instantiation. |
| 11 | **ChameleMon** (SIGCOMM'23, DOI 10.1145/3603269.3604850; **A** + arXiv HTML sections) | Packet loss (victim flows) + heavy hitters via FermatSketch | **Central controller** reconfigures division points / thresholds T_h, T_l; switch encodes (§4.3) | Number and ratio of victim flows (healthy vs ill state) (§4.3.1–4.3.2) | Switch memory split between HH / heavy-loss / light-loss encoders; collector bandwidth 0.8 % of a 40 G NIC, 50 ms epochs (§5.2) | No | N (ECMP fat-tree) | 10× Tofino, 4 ToR, 8 servers (§5.2) | 2-level fat-tree; lossless not discussed | 15.9×/23.2× less memory than FlowRadar/LossRadar; attention shifts within ≤ 3 epochs (150 ms) (§5.2) | "'Shifting measurement attention as network state changes' is literally ChameleMon's title." | ChameleMon's attention is over **tasks and flows** (memory between loss vs HH), driven by a controller at 50 ms epochs, on ECMP. Ours is over **paths (dst-leaf, spine)** under spraying, updated in the data plane per packet, gating *mirroring/INT/tagging* rather than sketch memory, priced across four resources per iteration. |
| 12 | **SAROS** (APNet'24, DOI 10.1145/3663408.3663429; **F** pp. 1–3) | Network-wide heavy hitters; KMV sampling with per-NMP dynamic threshold T_j (Alg. 1) | Data plane samples; **control plane predicts next-interval threshold from history** and pushes it (Alg. 2, §3.2) | Historical k-th minimum hash value per NMP | Sampled-packet volume to controller; Bloom filter for dedup | Time-series prediction (no ML stated) | N (routing-*oblivious* ≠ spray-aware) | Programmable switch prototype (public) | n/a | F1 +10–40 %; memory −99 % vs AROMA (§1) | "Control-plane-predicted, data-plane-enforced adaptive sampling thresholds per measurement point already exist." | Per-switch threshold for HH detection; no per-path state, no failure objective, no budget pricing, no NIC loop. |
| 13 | **HeteroSketch** (NSDI'22; **A**) | Sketch-task placement + resource allocation across heterogeneous programmable devices | **Controller optimiser** + profiler; re-solves on topology/traffic/query/resource dynamics | Device capability profiles, monitoring goals | Device CPU/memory/throughput; −20–60 % resource overhead | No | N | Switches, SmartNICs, software switches; > 40 k-node scale via clustering | n/a | 20–60 % resource savings at equal accuracy | "Network-wide placement and resource allocation under dynamics is HeteroSketch." | Periodic offline optimiser for sketch placement; no in-data-plane adaptation, no path uncertainty, no failure localisation, no in-band NIC evidence. |
| 14 | **PReCCL** (SIGCOMM'26, DOI 10.1145/3789240.3829133; **A**) | Software in-band telemetry inside a drop-in NCCL replacement: stall counts per **virtual topology (VT)** | **Host / collective library**; reallocates workload from stalled VTs to healthy ones | Stall counts | None stated | No | Partial (VT-level, not per physical path) | GPU clusters 32–1024 GPUs | Not stated | Up to 2.1× CCT, 1.21× end-to-end training | "PReCCL already senses per-collective anomalies in-band and routes around them from the host." | Host-only, VT granularity (not spine/link), *avoids* rather than *localises*; no switch state or budget. PReCCL is a downstream consumer of our localisation, and evidence that per-iteration telemetry is the right cadence. |
| 15 | **Themis** (EuroSys'26, "Enabling Packet Spraying over Commodity RNICs with In-Network Support"; **F** pp. 1–6) | Per-QP per-path PSN state on ToR: Proxy Bitmap + **PathMaxPSN table** to classify NACKs valid/invalid/undetermined (§4.2, Alg. 1) | **ToR switch** (Themis-S deterministic PSN-based spraying, Themis-D NACK validation) | OOO-induced NACKs; path inferred from PSN mod N (§4.1, Eq. 1–3) | None (reliability middleware) | No | **Y** (PSN-based spraying) | Tofino2 + ConnectX-7 (§5, §6.1); NS-3 16×16 leaf-spine (§6.2) | 2-tier (3-tier via PathMap, §4.1); explicitly targets **PFC-free lossy** fabrics (§2.2) | CCT −22.7–35.4 % Allreduce, −29.3–47.3 % Alltoall; only 8 % tail-CCT degradation under ToR–spine link failure (§1) | "Themis already keeps per-QP, per-path PSN state on the ToR and infers per-path loss from NACKs — that *is* your PSN-gap evidence." | Themis uses that state for **reliability** (filtering spurious NACKs), not for measurement, and has no budgets or actuation. We reuse its PathMaxPSN idea as the *source* of PSN-gap evidence and cite it as a building block; the contribution is what we do with the evidence. |
| 16 | **R-Pingmesh** (SIGCOMM'24, DOI 10.1145/3651890.3672264; **F** pp. 554–558) | Network RTT + end-host processing delay via UD-QP probes; RNIC vs in-network drop attribution; service tracing on live 5-tuples (§3.2, §4.2.1) | **Controller** generates ToR-mesh and inter-ToR pinglists; k 5-tuples per ToR pair chosen to cover N ECMP paths with P = 0.99 (Eq. 1, §4.1) | Periodic; 5-tuples rotated periodically (§4.1); not adaptive to uncertainty | Probe interval per RNIC from expected per-link probe frequency (§4.1) | No | **N** — ECMP; 5-tuple hashing coverage breaks under spraying (the OPP motivation) | Commodity RNICs; tens of thousands of RNICs, 6 months (§1) | Clos; lossless RoCE | 85 % of located problems accurate; all 157 switch problems accurate (§1) | "Production-scale, service-aware RoCE monitoring already exists." | Host-only probes, ECMP-only coverage model, controller-static pinglists, no in-network state, no budget adaptation. Under spraying its coverage guarantee (Eq. 1) no longer holds. |
| 17 | **Hostmesh** (APNet'24, DOI 10.1145/3663408.3663426; **F** pp. 1–4) | One-way latency, one-way drops, end-host processing delay by **full-mesh probing between RNICs on the same host** (§3.3) | Agent per host, **no controller**; source-port rotation for ECMP coverage; traceroute rate-limited (§3.3.2) | Periodic | Traceroute frequency limited to protect switch CPU (§3.3.2) | No | N (ECMP, rail-optimised only) | Commodity RNICs; hundreds of servers, 3 months | 2-tier rail-optimised | 8 problem types located | "Controller-free host probing already covers all NICs/links." | Same as R-Pingmesh; additionally limited to rail-optimised topologies. |
| 18 | **INSERT** (INFOCOM'24; **A**) | End-to-end RDMA telemetry: eBPF tracing on hosts + **stateful per-QP RDMA telemetry on SmartNIC data plane**, robust to OOO packets | NIC data plane (fixed) | None | None stated | No | Partial (handles OOO) | Programmable SmartNIC | n/a | Line-rate accurate telemetry across RDMA traffic types | "Stateful RDMA telemetry in a programmable data plane already exists." | NIC-local visibility; no path/spine attribution; no budgeting. We *consume* this class of NIC state. |
| 19 | **PINT** (SIGCOMM'20, DOI 10.1145/3387514.3405894; **A**/K) | INT with bounded per-packet overhead by spreading data over packets (down to 1 bit; 16 bits for CC/path tracing/tail latency) | Fixed probabilistic encoding at switches | None | Per-packet bit budget (uniform across packets) | No | N | P4 | n/a | Matches SoTA at 16 bits/packet | "Bounded-overhead INT is solved." | PINT bounds *bits per packet uniformly*; it does not decide **which paths** deserve bits, is not failure-aware, and has no cross-resource pricing. PINT-style encoding can be one of our actuators. |
| 20 | **Sel-INT** (TNSM'20, DOI 10.1109/TNSM.2019.2953327; **A**) | Selective INT with runtime-programmable sampling rate and data types | Operator/controller sets rate at runtime | Manual/runtime policy | INT overhead | No | N | POF-extended OVS (software) | n/a | Adjusts rate/types at runtime with acceptable accuracy | "Runtime-adjustable INT sampling exists." | Global, operator-set rate; software switch; no per-path, no learning, no failure objective. |
| 21 | **AdapINT** (arXiv 2310.19331; **A**) | Auxiliary probes (coverage via DFS) + dynamic probes (task-specific) | **Controller**; DRL + transfer learning plans dynamic-probe paths | Telemetry requirements + network dynamics | Telemetry latency, control overhead (−75 % / −34 %) | **Yes — DRL** | N | Not stated | Not stated | Latency −75 % (games/video), control overhead −34 % (cloud) | "RL-driven adaptive INT exists." | Controller-side probe *routing* for coverage; no data-plane attention state, no spraying, no NIC evidence, heavyweight DRL vs our lightweight bandit. |
| 22 | **INTaaS** (Computer Networks 241, 2024, DOI 10.1016/j.comnet.2024.110211; **A**) | INT service provisioning: INT path-algorithm selection + query-workload distribution | **Controller**; online **primal–dual** learning on a time-varying integer programme + randomised rounding | Observed query workloads / QoS | INT collection capacity coupled over time | **Yes — online primal–dual (dual variables = prices)** | N | Barefoot Wedge100BF + INTCollector | n/a | Up to +40 % QoS over SoTA | "Online primal–dual pricing of INT resources is INTaaS — your shadow-price bandit is that." | INTaaS prices *INT service capacity among tenant queries* at the controller; not failure-directed, not per-path, no in-data-plane attention, no NIC loop. We share the Lagrangian pricing idea (must cite) but couple prices to switch-local uncertainty state and price four heterogeneous resources per collective iteration. |
| 23 | **FANcY** (SIGCOMM'22, DOI 10.1145/3544216.3544242; **A** + K) | Gray-failure loss via **inter-switch counter synchronisation** protocol; (K) hierarchical "zoom" of counters onto lossy prefixes | Switch data plane (Tofino) | Counter mismatch between neighbouring switches | Counter memory (K: zoom tree bounds it) | No | N | Intel Tofino | ISP (not DC); loss-only | Detects gray failures in seconds for tiny loss fractions; enables fine-grained fast reroute | "Adaptive in-data-plane zooming onto lossy prefixes is FANcY." | ISP/prefix-oriented, loss-only counters, no NIC signals, no spraying, no multi-resource budget. FANcY's zoom is the right precedent for *data-plane attention* and we cite it as such; ours targets paths under spraying with multi-signal evidence. (Verify zoom details before citing specifics.) |
| 24 | **Alibaba Stellar** (SIGCOMM'25, DOI 10.1145/3718958.3750539; **A**) | Not a telemetry paper: PVDMA, eMTT, **RDMA Packet Spray** | NIC/virtualisation stack (K) | n/a | n/a | No | **Y** (RDMA Packet Spray) | Alibaba cloud AI clusters | Not stated in abstract | Container init 15× faster; LLM training up to +14 % | "Production RDMA spraying already exists." | Motivation only: confirms spraying is production reality; contributes no measurement mechanism. |
| 25 | **UEC 1.0 / UET** (spec June 2025; via "Ultra Ethernet's Design Principles" arXiv 2508.08906, **F**) | Per-EV (entropy value) spraying at the FEP/NIC (§2.1); ECN 1-bit + RTT multi-bit congestion states (§3.3.1); **probe control packets along specific EVs** (§3.2.4); optional packet trimming; optional CBFC lossless (§3.5.2) | **NIC** (load balancing at CCC level, §3.3.5); switch telemetry optional | Transport CC signals | None (spec defines mechanisms) | No | **Y** | Spec; many vendors | Lossy default, lossless optional | n/a | "UET already has per-EV probes, trimming NACKs and optional switch telemetry; the standard covers your loop." | UET defines *mechanisms* and leaves policy open; there is no switch-side per-path uncertainty state and no budgeted allocation of telemetry. We are a UET-compatible controller that decides *when* to spend those mechanisms. |
| 26 | **REPS** (arXiv 2407.21625, EuroSys'26; **F** via arXiv HTML) | ECN-marked ACKs → drop EV; cache good EVs in 8-entry ring, ~25 B/connection (§3.1, §3.3) | **NIC** | ECN bit on ACK; timeouts for failure ("freezing mode") | None | Exploration then exploitation of EVs (§3.1) | **Y** | htsim; FPGA RDMA NIC testbed, 2-tier 100 G (§4.4) | 2- and 3-tier fat-tree, 1:1–4:1 (§4.1); OOO transport assumed, lossy | < 100 µs failure reroute; 100× better than OPS under transient link failures; 2.5–70× fewer drops (§4.3.3, §4.4.3) | "The NIC already learns per-path quality and avoids failing links in 100 µs; localisation is moot." | REPS *masks* failures — the operator never learns which link is sick, so the fabric silently loses capacity. Binary ECN, no attribution. This is the strongest motivation for C3: the NIC's avoidance decisions are exactly the evidence we reflect to the switch to localise and repair. |

## Part B — papers found that are closer to the thesis than parts of the assigned list

| # | Paper (venue/yr, ev.) | What is measured | Where decided | Signal | Budget | Learning | Spray | HW | Topo/loss | Headline | Reviewer sentence | Rebuttal |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 27 | **FANT: Flexible Attention-Shifting Network Telemetry** (Applied Sciences 15(2):892, 2025, DOI 10.3390/app15020892; **A**) | Active INT (AINT): "basic" lightweight metrics on all links, "detailed" metrics only on links the attention mechanism selects each cycle | Controller/probe planner per measurement cycle | Criticality of links from basic measurements | Probe bandwidth / compute | Not stated | N | Not stated (likely bmv2/Tofino — unverified) | Not stated | "Significant" bandwidth and compute reduction | "Attention-shifting telemetry that focuses detail on critical links each cycle exists — and uses your word." | Controller-driven probe telemetry with a two-tier metric menu; no data-plane per-path state, no spraying, no failure objective, no learned budgets, no NIC. Must be cited to defuse the name collision. |
| 28 | **Resilient AI Supercomputer Networking using MRC and SRv6** (OpenAI, arXiv 2605.04333; **F** via HTML) | NIC keeps "a few bits of state about path health" per EV; ECN echo; non-trimmed loss ⇒ path removed (§2.1); NIC background probes; **Clustermapper agent probes every link every millisecond** (§3) | **NIC** + external agent | ECN, loss per EV | Fixed ms-level full-mesh probing | No | Y (NIC spraying over SRv6 static routes) | CX-8 NICs; 42 K-GPU runs | Two-tier multi-plane, 131 K GPUs; **lossy, PFC off** (§2.1) | 770 Gb/s p2p; 92 GB/s NCCL at 42 K GPUs; ride-out of T0–T1 failures (§5.2) | "OpenAI brute-forces it: probe every link every ms; adaptive budgets are unnecessary at hyperscale." | Fixed-budget, agent-based probing over *static* source routes — you can source-route a probe only because spraying is disabled at the switch. Under switch-side adaptive routing that option vanishes; our budget-adaptive in-network attention is the replacement. Also confirms lossy-by-default (C2). |
| 29 | **Pulse** — "Fine-grained and Non-intrusive LLM Training Monitoring via Microsecond-level Traffic Measurement" (ASPLOS'26, DOI 10.1145/3779212.3790163; **A**) | µs-level RDMA flow measurement on NICs → collective-operator-level monitoring; 2000 concurrent flows per NIC | Host/NIC | Flow-level timing | Negligible overhead claimed | No | N/partial | 64× H200 testbed | n/a | Machine-level localisation in 10/12 failure scenarios; > 90 % precision, 100 % recall | "NIC-side µs measurement already localises failures in LLM training." | Host/NIC-only, machine-level not link-level; no switch cooperation; no budget. Complementary evidence source. |
| 30 | **Rapid Detection and Localization of Gray Failures in DCs via INT** (NOMS 2020, Xplore 9110326; **A**) and follow-up **GrayINT** (Xplore 10258170) | INT probe packets build per-server path tables; failure = path entries timing out | Servers + INT probes | Probe timeouts | None | No | N | P4/bmv2 (K) | Clos | Rapid detection/localisation (numbers not retrieved) | "INT-based gray-failure localisation in DCs is old." | Probe-based, uniform, ECMP-era; no adaptive budget, no spraying, no NIC evidence. |
| 31 | **Self-Adaptive Dynamic INT Orchestration for Balancing Accuracy and Stability** (TNSM 2025, DOI 10.1109/TNSM.2025.3530432; **A**) | Network-wide INT orchestration trading accuracy vs the instability caused by re-planning collection | Controller | Accuracy/stability trade-off | INT bandwidth | Not stated | N | Not stated | n/a | Not retrieved | "Adaptive INT orchestration under an overhead budget exists." | Controller-side path planning of INT probes; not per-path attention, not failure-directed, no NIC. |
| 32 | **DiSketch** (CoNEXT'25 CNC workshop, DOI 10.1145/3769270.3770744; **A**) | RL agent for resource-aware distributed sketch deployment | Controller | Device resources | Sketch memory/compute | **Yes — RL** | N | Not stated | n/a | Not retrieved | "RL already allocates telemetry resources across devices." | Placement problem, not per-path measurement attention; same gap as HeteroSketch. |
| 33 | **Varuna: Failure-Type Aware RDMA Failover** (arXiv 2603.28001; **A**) | Completion log classifies in-flight RDMA requests as lost vs executed on link failure | NIC/host | Failure event | none | No | N | Primary+standby links | n/a | −65 % recovery retransmission time | "Failover already handles link failures." | Recovery, not detection/localisation; orthogonal. |
| 34 | Adjacent (cite, not threats): **Lumina** (SIGCOMM'23) Tofino mirroring to study RNIC micro-behaviour; **NetSeer** (SIGCOMM'20) flow-event telemetry; **Flock** (arXiv 2305.03348) probabilistic fault localisation at scale; **µMon** (SIGCOMM'24) in-dataplane wavelets. | — | — | — | — | — | — | — | — | — | — | None claims adaptive, budgeted, spray-aware, NIC-coupled attention. |

---

## Historical closest prior work, ranked against the retired thesis

1. **SprayCheck (arXiv 2605.03702, 2026)** — same problem (gray failures under adaptive routing), same hardware class (Tofino + CX-6, 2-level fat-tree), in-switch, coordination-free. Every axis it leaves open is ours: loss-only signal, lossless-only, 2-level only, round-robin coverage, no budget. FlowPulse (HotNets'25) is its precursor and shares the gap. **This is the paper reviewers will name; our C1 baseline B2 must be its exact round-robin.**
2. **OmniPath Ping (SIGCOMM'26)** — the only *measurement primitive* explicitly built for spraying, with an in-network cache and an SRAM-vs-timeliness knob. It is uniform-coverage and probe-based; it is our C1 baseline B3 and an arm of our bandit.
3. **ChameleMon (SIGCOMM'23)** — the canonical "shift measurement attention with a controller loop on Tofino" paper. Attention over tasks/flows and sketch memory, ECMP, 50 ms epochs. We must show attention over *paths* under spraying, in SALUs, gating mirror/INT/tag, priced across resources.
4. **FANT (Applied Sciences 2025)** — name collision on "attention-shifting telemetry"; controller-side probe planning. Cite to neutralise.
5. **INTaaS (Computer Networks 2024)** — online primal–dual (shadow-price) provisioning of INT; closest to our control-plane mechanism, but not per-path, not failure-directed, no data-plane state.
6. **BRIGHT (ICACT 2026)** — Bayesian posteriors + RL for uncertainty-directed monitoring; OpenFlow polling. Closest to our *uncertainty* framing.
7. **Themis (EuroSys'26)** — per-QP per-path PSN state on ToR under spraying; the source of our PSN-gap evidence, used there for reliability not measurement.
8. **NIC-side transports — MetaRoCE, UET/REPS, OpenAI MRC** — hold the per-path evidence and mask failures; they define what the NIC half of C3 looks like and why the switch half is needed.
9. **FANcY (SIGCOMM'22)** — data-plane zooming toward lossy regions; ISP, loss-only.
10. **DynATOS/DynATOS+ (NSDI'22 / ToN'24), HeteroSketch (NSDI'22), CoordSamp (ToN'26)** — resource budgeting of telemetry under dynamics, all controller-side and path-agnostic.

## Retired gap statement

The 2026-08-25 review proposed that no published system combined switch-local path uncertainty,
selective live-traffic telemetry, reflected NIC evidence, and controller-side resource pricing. The
subsequent gate showed that a conjunction of known mechanisms is too slim a novelty basis:
Bellman/Blackwell occupies the scheduling result, SprayCheck occupies passive localization under
spraying, and NetSeer/LinkGuardian/UEC occupy the link-local witness. This paragraph is retained
only to document the rejected direction.

## Provisional replacement gap — must pass the new gate

The candidate gap is the **post-mitigation lifecycle**, not initial detection. Adaptive routing,
NIC path avoidance, or quarantine can remove production traffic from a suspect link and therefore
remove the passive evidence used to tell whether it is still faulty, recovered, or flapping.
Existing rows already cover passive detection, service tracing, generic active probing, liveness
traffic, mitigation, and telemetry scheduling. The narrower hypothesis to test is whether prior
work already provides the full contract below:

1. mitigation creates the missing-evidence condition explicitly;
2. an audit exercises one avoided directed link;
3. the switch enforces the audit packet/byte exposure cap despite sender or controller oversend;
4. restoration requires fresh, confidence-qualified evidence and includes probation/relapse.

This is **not yet an absence claim**. Task 1 in
`docs/superpowers/plans/2026-08-28-counterfactual-observability.md` must add rehabilitation,
revalidation, quarantine/probation, safe-exploration, and post-reroute diagnosis work to this
matrix and issue `PASS`, `NARROW`, or `FAIL` before the primitive is presented as novel.

---

## Zotero status

All 39 items were filed in collection `MCP-sprayed-fabrics` (`AT2STS8I`); `zotero_find_duplicates` on the collection reported none. PDFs attached only for arXiv items (SprayCheck, CoordSamp, AdapINT, UE design principles, REPS, Varuna, MRC/SRv6, Flock); ACM/IEEE items are metadata-only (open-access download blocked with 403). Items saved as webpages because no DOI resolved: MetaRoCE blog, IETF CSIG draft, DynATOS (USENIX page), DynATOS+ Xplore page, HeteroSketch (USENIX page), Themis (HKUST PDF URL), UEC v1.0 page, INSERT (ResearchGate).

## Full-text access gaps to close before citing numbers

OmniPath Ping, CSIG (SIGCOMM'26 paper), PReCCL, Stellar, FANcY, HeteroSketch, DynATOS(+), FANT, BRIGHT, INSERT, INTaaS, CPRANT (no abstract anywhere). All are behind ACM DL / IEEE Xplore 403 for this fetcher; a library VPN or the authors' pages will resolve them.

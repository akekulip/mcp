# HURDLES — known risks, mitigations, and the test that closes each

| # | Hurdle | Mitigation | Closed by | Status |
|---|---|---|---|---|
| H1 | 12 MAU stages; hop-fwd + spray + counters + drop + INT + attention gate may not fit | Single `vlink_id` index computed once; range tables for compares; INT insertion in egress only; compiler resource report | S-DOWN compile (LOCAL bf-sde-9.13.1 + tofino-model + PTF at /home/philip/bf-sde-9.13.1 — verified 2026-08-25) | closed 2026-08-26 for the full program: all §9.2 steps compile in 9 ingress + 3 egress stages on 9.13.1 and 9.13.2 (p4/reports/step7.md); no headroom left in ingress — next feature must reuse a stage |
| H2 | Recirc bandwidth: H hops × 35 G host ingress | dp68 + spare ports in MAC-near loopback; cap H ≤ 4 for host traffic; pktgen for stress | S-DOWN design, S-UP measure | open |
| H3 | Max 4 RegisterActions/Register; 32-bit SALU; no var-vs-var `if` | Split state; range keys; widen flags to bit<8> | S-DOWN compile (LOCAL bf-sde-9.13.1 + tofino-model + PTF at /home/philip/bf-sde-9.13.1 — verified 2026-08-25) | open, unblocked |
| H4 | No TNA INT/qdepth precedent locally; PHV budget | Fixed 8–12 B CSIG-style compare-and-replace tag, not INT stack | S-DOWN compile + PTF | closed 2026-08-27 on silicon: 14-B CSIG tag inserted in ingress, compare-and-replaced in egress; worst_vlink correct 269/269 copies, qdepth tracks a shaped uplink (p4/reports/step5-7-silicon-v2.md) |
| H5 | Mirror session count / truncation on 9.13.2 | `$max_pkt_len` code from defense4; one session per attention class | S-DOWN scripts, S-UP verify | closed 2026-08-27 on silicon: sessions 1 (128 B) / 3 (64 B) to dp9, copies carry a 24-B mirror_h with the pass verdict; fault copies == inj_drop count exactly; truncation lengths as predicted |
| H6 | rxe single-core ceiling; no DCQCN/PFC | Many QPs + netns; state lossy/software-RDMA; DPDK generator | S-DOWN on Agilio↔Hulk link | open |
| H7 | Agilio PCIe x4 (31.5 G), 10 G lanes; SDK absent (verified 2026-08-25: no /opt/netronome, no p4c-nfp; `nfp` driver loaded, device 4000, lanes enp175s0np{0,1}s{0-3}) | XDP (NFP offload if firmware allows, else host XDP); RTT via host XDP | S-DOWN on Vision | closed 2026-08-28: XDP route PROVEN on the Agilio — eBPF counter offloaded to the card (JIT 864 B, map on-card, 160 pkts binned exactly across 16 paths); native P4-on-NFP remains impossible (no Corigine SDK anywhere). Procedure + traps: nic/README-xdp.md  Native P4 route researched 2026-08-28 (docs/NETRONOME-SDK.md): toolchain is the Agilio P4C SDK, not on GitHub/any public repo; Netronome apt repo is live but carries BSP only; vendor licence ships it WITH the hardware, so the route is a support request to Corigine (draft in docs/netronome-sdk-request.txt) |
| H8 | Single TM ⇒ no cross-switch skew | State in paper; sim tier covers | — | accepted |
| H9 | Sim→HW gap | Calibrate htsim with measured RTT / counter-read latency; KS distance | S-DOWN partial, S-UP | open |
| H10 | Loopback-mode change silently rejected on live entry | DELETE-then-add | S-UP | open |
| H11 | bf_switchd exits on stdin EOF; LD_LIBRARY_PATH | `launch_switchd.sh` | S-UP | closed 2026-08-26: launch_mcp_switchd.sh (LD_LIBRARY_PATH + never-closing stdin) works; bf_kdrv must be reloaded after a switch reboot |
| H12 | Shared chip (`defense4_caseA` loaded) | tofino-p4 shared-chip rules; schedule windows | S-UP | moot for now: chip free since the 2026-08-26 reboot (gc-switchd masked, defense4_caseA not loaded); mcp_fabric owns it — hand back by unmasking gc-switchd when asked |
| H13 | Reward leakage / non-stationarity | Observation-only reward + unit test; discounted UCB baseline | S-DOWN sim | open |
| H14 | Stale skill facts (switch IP .15 → .81) | Updated tofino-p4 skill + lab-servers memory 2026-08-25 | S-DOWN | closed |
| H15 | Deadline | SIGCOMM'27 confirmed by Philip 2026-08-25; CFP not yet out, assume ~late Jan 2027 by '26 pattern — re-verify monthly | M0 | closed |
| H16 | Hosts lack `rdma_rxe`, perftest, DPDK (verified 2026-08-25; libibverbs 50.0 present, kernel 6.8, bpftool present) | `modprobe rdma_rxe` + `rdma link add` + apt perftest/dpdk — needs sudo on shared lab servers → get Philip's go-ahead | S-DOWN | approved 2026-08-25, in progress |
| H17 | Local SDE is 9.13.1, switch runs 9.13.2 — version drift | Iterate locally; final compile on the SDE host before any silicon run; record both hashes | S-UP | closed for P4 step 4: 2026-08-26 compile on the switch SDE 9.13.2 (p4c SHA 1baf055) = 0 errors, stages 0–6, 23 tables, tofino.bin identical size to the 9.13.1 build; re-check after each P4 step |
| H18 | htsim GOAL run of Llama-7B (2n×4g, 128-node fat tree) exceeds 10 min per seed; LULESH/incast run in seconds | Use LULESH/incast + Chakra-generated collectives for 30-seed sweeps; reserve Llama for a few confirmatory runs; consider smaller topo/`-end` | S-DOWN sim | open |
| H19 | `htsim_uec -goal` segfaults on 2-tier `leaf_spine_tiny.topo` (uec.cpp:3042) | Use 3-tier fat trees; report upstream | S-DOWN | accepted |
| H20 | rxe is go-back-N: per-packet spraying → continuous NAK/retransmit; PSN-gap evidence confounded by reordering (PRE-REVIEW R2) | Measure NAK/retx floor under netem reorder on the direct link now; on fabric, cap spray fan-out or use flowlet mode for rxe; report floor as evidence noise | S-DOWN (netem) → S-UP | open |
| H21 | No ECN/DCQCN on Soft-RoCE → NIC arm has RTT + PSN gaps only; must not be called "MetaRoCE-like" | State channels explicitly; ECN evidence sim-only until libsoftmetaroce/real RNIC | paper | accepted |
| H22 | No autonomous in-switch attention update rule specified (PRE-REVIEW R1) — thesis undefined until it is | Write rule (SALU: `attn += k` on threshold/NIC evidence, `attn -= 1` per N clean, clamp); pre-register; add fast-loop falsification + µs reaction-latency hypothesis | S-DOWN design + PREREG | closed 2026-08-26: rule frozen as PREREG amendment v1.3, implemented in mcp_fabric.p4 step 5 (8 stages, compiles on 9.13.1 and 9.13.2) |
| H23 | dp65 (33/1) Agilio leg existence unverified; connectivity map deliberately excludes it | Physical check when switch is up; design collector port fallback | S-UP | open |
| H24 | SprayCheck absent from hardware arms; OPP has no arm; localizer confound; H2 unpassable; no background-loss sweep; NIC-only arm forbidden from aggregating (PRE-REVIEW R2/R3/§4) | PREREG amendment in progress | S-DOWN | open |
| H25 | htsim `main_uec.cpp` derives min RTO from queue size in bytes: with the gate's `-q 1e6` pkts (4 GB) min RTO ≈ 2 s > `-end`, so `startRTO()` never arms and a silently lost data packet is never retransmitted (incast seeds 11/26 stalled forever, GOAL loop spun to a 6.5 GB log; receiver rx bitmap wraps at 16384 in-flight). Sleek/NACK paths do not cover silent loss (2026-08-26) | `-rto_min_us` flag (sim/htsim 83df0ee), `RTO_MIN_US=300` in run_gate.sh; per-run timeout + log cap + `.STALLED` marker; rerun 105/105 clean. Re-check whenever `-q`, MTU or link speed change | S-DOWN sim | closed |
| H26 | htsim memory grows with simulated time on MoE GOAL traces: ~20 GB peak per 3.5 s MoE-64 run on the 1024-NIC fat tree (18.5 GB at 3.4 s; LULESH stays small) — per-flow UEC state for ~1e5 flows is never released, so fleet width is memory-bound (Hulk 5 / Vision 10 concurrent), ~9 h for the 90-run gate (2026-08-26) | Size fleets by `seed<N>.time` maxrss; investigate freeing finished UecSrc/UecSink (or trimming `-paths`, rx bitmap) upstream; larger traces (MoE-128, 10 GB bin) need the 246 GB host alone | S-DOWN sim | open |
| H27 | Fault observability is traffic-phased: on MoE-64 the faulty uplink US0->CS0 carries traffic only at 1.5–2.0 s and 3.3–3.6 s of the 3.5 s iteration, so all silent drops (~700 over 35 runs) fall in epochs 15–19 and 33–35; oracle detects at the first drop epoch (16–17) in 5/5 seeds and uniform/oracle TTL has CV ≈ 0.03 — TTL measures the collective schedule, not the policy; counters: 896/1024 uplinks idle in epoch 10, all busy in epoch 16 at ~6–7 % load (2026-08-26, 4 % pilot) | PREREG: define TTL from first *observable* drop (fault-visibility-adjusted) or from onset with onset placed inside a traffic window; randomize the faulty uplink per seed (H-order confound: US0->CS0 is first in uniform's sweep); consider multi-iteration horizons or a background-load trace so links carry continuous traffic | S-DOWN sim | open |
| H28 | Frozen-localizer warm-up counted UPDATE CALLS, not evidence: `baseline_warmup_epochs = 10` gated the CUSUM on ten pool updates, so a schedule collecting every 4th epoch waited 4x longer for the same packets and the H8 in-band arm read 11 drops in 99,704 packets with CUSUM 0.00. The defect is not neutral across arms — it penalises exactly the low-cadence collection the thesis proposes (2026-08-28) | Warm-up defined in observed packets: `baseline_warmup_packets = 1e5` (= 10/δ, so baseline noise ~1/N = 1e-5 sits an order below the detected shift) plus `baseline_warmup_latency_samples = 10`; `ElementState.n_pkt_loss` / `n_samp_lat` added; PREREG v1.6; infer.py re-frozen `be12e7b2` → `0a989aaf`; regression test feeds the same evidence in a quarter of the calls and requires detection. Every replay result re-issued | M1 | closed |
| H29 | Three silent defects in the replay harness found in the same pass, each changing what a published number MEANS: semi-synthetic fault identities drawn from Python's per-process-salted `hash()` (multi-fault scenarios not reproducible between runs of the same command); multi-fault success semantics implicit ("the recorded fault, distractors ignored") while reported as if general; and the oracle handed only the RECORDED fault, so under the "all" objective it was not an upper bound — uniform 25.0 beat oracle 26.0, which is the tell (2026-08-28) | CRC-32 `scenario_seed()`; explicit `--objective any\|all\|original` printed in every run header; oracle reads every injected fault; H9 gate line now evaluates the paired significance test as well as the 30 % margin and ranks only counter-computable schedules. Lesson: an upper-bound arm that loses to a baseline is a harness bug until proven otherwise | M1 | closed |
| H30 | `pkill -f <pattern>` kills the issuing shell whenever the pattern appears in its own wrapped command line — the same failure that kills SSH sessions, seen again today when a cleanup `pkill -f "replay.py --results …"` terminated the patch script mid-run and left the edit unapplied (2026-08-28) | Use `pkill -x <exact-name>`, or match on a PID from `pgrep` filtered by `$$`; never put a long argument string in `pkill -f` from an interactive or scripted shell. Verify the edit landed after any kill | tooling | closed |
| H31 | Launching a second htsim batch on a host already running one over-subscribed memory: Vision reached **7 GB free of 250** with 16 concurrent runs (10 surplus F0 policy variants at 150 GB + 6 fault-plus-background runs at 89 GB), heading for an OOM that would have killed both cohorts. Killing the surplus *runs* did not help — `run_gate_real.sh` drives its children through `xargs -P`, so the pool immediately respawned replacements (2026-08-28) | Stop the LAUNCHER first, then its children. The two launchers have identical command lines (`bash ./run_gate_real.sh`; the output directory is an environment variable), so distinguish them by `tr '\0' '\n' < /proc/<pid>/environ \| grep '^OUT='` rather than by `pgrep -f`. Size a second batch against *peak* footprint (~21.5 GB/run, H26), not against current free memory: 16 runs need ~344 GB on a 250 GB host. Nothing was lost here because the planned 20 background-loss and 10 clean F0 seeds were already complete and copied off; the killed runs were redundant policy variants, and F0 counter logs are arm-independent by construction | S-DOWN sim | closed |
| H32 | `-mcp_loss` (the F1 fault) **overwrote** `-mcp_bg_loss` instead of composing with it: `Pipe::set_loss_prob` REPLACES `_loss_prob`, so on a fabric with 1e-4 background a 1e-4 fault left the faulty link at exactly 1e-4 — identical to every other uplink and invisible by construction. Measured on the first coexistence runs: the faulty link's rate was **0.72x and 0.92x the uplink mean** and it ranked 958th, 681st and 522nd of 2048 links by loss rate. Every arm censored 6/6 at every threshold from 2.0 to 6.5, INCLUDING the oracle, which is the tell — an upper-bound arm that cannot find a fault it is handed means the fault is not there (2026-08-28). The source comment claimed "fault on top of background", so the intended semantics were documented and the code did the opposite | `sim/htsim` commit `3cf9a15` composes them as independent Bernoulli losses, `p_eff = 1 - (1-p_bg)(1-p_fault)`, and prints the effective rate; verified live: `MCP silent loss p=0.0001 … (effective p=0.00019999 composed with background 0.0001)`. Cohort regenerated. **Lesson, and it is the same one as H29:** when an oracle handed the answer cannot find it, suspect the harness before the detector — and check the injected quantity in the data, not in the flags | S-DOWN sim | closed |
| H33 | The C-W4 witness manufactures phantom loss out of pure reordering, and the first dynamic harness modelled it as harmless. `reg_wit_expect` (`p4/witness/mcp_fabric_gate_event.p4:749`) is `rv = v - hdr.witness.seq; v = hdr.witness.seq + 1` — the resync is **unconditional**, and arming is `if (md.wit_gap != 0)`, so every non-zero gap emits. Emulating that RegisterAction exactly (emulator first validated against `p4/ptf/gap_event/test.py::Test50`, which it reproduces: arrivals 0,1,4,5 -> one event, gap 0xFFFE, lost 2), a single adjacent swap with **zero packets actually lost** yields THREE events — `0xFFFF` (read as lost=1), `0x0002` (lost=0), `0xFFFF` (lost=1) — and two swaps manufacture a phantom `0xFFFE` (lost=2). The harness modelled one swap as a single `gap=2, lost=0` event, crediting reordering with zero false evidence and making PREREG decision rule 2 ("`reorder_only` produces zero quarantines") unfailable by construction (2026-08-29) | Derive loss and reorder from ONE witness mechanism in `sim/dynamic/fabric.py` — apply the register action to the actual arrival order — rather than special-casing reorder; keep `observed_packets` consistent with `reg_wit_observed` (line 763), which returns the pre-update value and resets to 0 whenever `gap != 0`. Scope limit, verified rather than assumed: `reg_wit_seq` (line 1230) stamps the sequence in EGRESS indexed by `md.sublink`, *after* `tbl_eg_vlink`, so a packet is numbered as it leaves on a chosen sublink in TM release order. Per-packet spraying selects the link before the sequence exists and therefore cannot reorder a sublink's own sequence — the detector is immune to spray-induced reordering **by construction**, which is a property the paper should claim rather than a risk it should hedge. Residual exposure is reordering within one physical link, which the harness must still be able to represent. **Lesson: the model of a mechanism must be derived from the mechanism's source, not from what the mechanism is for.** A hand-written special case for the benign path is where a harness quietly stops being able to produce a bad result | S-DOWN sim | fix in progress 2026-08-29 |
| H34 | **Restoration counts rounds, not evidence — the H28 defect class, in a second place.** `SublinkFeedback.on_clean_epoch` requires `observed_packets > 0` and then increments a counter; restoration needs `clean_epochs_to_restore x quarantines` clean *rounds*, discarding how much evidence each round carried. `tbl_audit_steer` holds 16 entries and `AuditRound` refuses more, so a round is at most 16 packets. Measured consequence: the frozen k=3 rule certifies a **still-faulty** 1e-3 sublink 95.3 % of the time (48 probation packets; `P = 0.999^48`), and the dynamic sweep records 10 unsafe restorations per 5 runs where the fault persists — PREREG decision rule 4 FAILS. The criterion is dimensionless: it cannot express the fault rate it is supposed to exclude (2026-08-29) | Size probation by evidence: require accumulated observed probation packets `N >= ln(alpha)/ln(1-p_target)`. At 16 packets/round and one round per 100 ms epoch this prices safe restoration — 1e-3 at 95 % costs 2,995 packets and 18.8 s; 1e-4 costs 29,956 packets and 187.3 s. That turns a failing rule into a cost model, which is what the lifecycle claim needed. **Lesson, and it is H28's:** any threshold expressed as a count of controller events rather than a count of observed packets is dimensionless and will silently mean different things at different rates. Grep the controller for every remaining `_epochs`/`_rounds` threshold and check each one carries evidence. **Audit done 2026-08-29 and it is clean:** the only dimensionless threshold in any decision path is this one. `infer.py` already counts in packets (`BASELINE_WARMUP_PKTS`, the H28 fix); `bench_feedback_path.py`'s warm-up is a benchmark artifact, not a decision; `epoch_loop.py`'s `--epochs` is a run duration; `hw_adapter.py:133` is a header-length bound. No third instance of the class exists today | M3 / P3 | open, fix proposed 2026-08-29 |
| H35 | **The audit path is an unauthenticated authorization boundary.** `mcp_fabric_gate_event.p4:1055` is `if (md.hop == 0 && md.is_audit == 0) { tbl_health_gate.apply(); }` — audit packets bypass the behavioural health gate by design, because that is how probation reaches a sublink the gate has emptied. `tbl_audit_steer` (line 898) admits a packet to that bypass on `(udp.dst_port == 4792, udp.src_port == declared_token)` alone, with no check that the packet came from the controller. Any host that can emit UDP to port 4792 with a guessed 16-bit token is therefore able to push traffic onto a link the system has just decided is faulty — and the proposed wildcard-steering optimisation (one entry, `src_port` unkeyed, to lift the 16-packet-per-round cap) removes even the need to guess (2026-08-29) | Add the controller's provenance to the steering key — `md.role` (or the exact ingress dev_port carrying the controller) — which keeps the table at ONE entry while making the bypass unreachable from the fabric's host ports. This costs nothing and is required BEFORE wildcard steering is adopted. For a security-venue framing this stops being a defect and becomes part of the threat model: a selective-drop adversary would want exactly this primitive, so the probe path must be authenticated, and the declared-token set must remain controller-private. Note also that `AuditRound.accept` already rejects undeclared and duplicate receipts, so receipt FORGERY is bounded today; it is packet INJECTION into the bypass that is open | P3 / security framing | open, found 2026-08-29 |
| H36 | **Every number in `docs/review/P3-DYNAMIC-RESULT.md` was produced against an UNCOMMITTED controller, and cannot be reconstructed.** The whole `sim/dynamic/` harness was written against a working-tree `controller/sublink_feedback.py`; the committed controller at `4fd737c` has no `gate_keys_for_sublink` at all (`git show 4fd737c:controller/sublink_feedback.py \| grep -c gate_keys_for_sublink` = 0), so the harness cannot even IMPORT against any committed state. A controlled attribution of "which controller version produced this table" is therefore impossible from git, and the published rule-2 figures (32/10/3/0) already turned out to be non-reproducible once the credit rule landed — the right explanation, but one that could not be proven from history (2026-08-29) | Commit `controller/sublink_feedback.py`, `controller/hw_adapter.py`, `p4/witness/mcp_fabric_gate_event.p4` and the `sim/dynamic/` tree BEFORE the next reported measurement, and record the controller sha256 in every result table the way `conf/infer/frozen.yaml` pins the localizer. A pre-registered project whose results cannot be tied to a source revision has the same defect the PREREG exists to prevent. **Lesson:** freezing the inference layer is not enough — anything a published number depends on needs a revision, not just a mtime | process | open, needs Philip's go-ahead to commit |
| H37 | **The headline 7x collateral advantage is mostly arithmetic, and the sensitivity that would defend it is not runnable.** Decomposed by the PI on 2026-08-29 and verified: `persistent_partial`, p=1e-3, h=6.5, 40 epochs, seed 1 gives cw4 collateral 1,510,436 against directed_w4 10,572,965 = **6.9999x**. Of that, **4.00x is the four-context split** — quarantine one context of four and you divert a quarter of the traffic, which follows from the frozen share vector rather than from any property of the mechanism. The residual 1.75x comes from directed_w4 recording 87 false-quarantine epochs against cw4's 0, which is itself near-definitional: sweeping every context on the vlink necessarily quarantines the three healthy ones. A reviewer reproduces this decomposition in ninety seconds. Compounding it, `RunConfig` exposes no `context_share`, so every published number assumes a uniform quarter and the obvious rebuttal — collateral reduction as a function of the *measured* share of the faulty behaviour class — cannot be produced with today's harness. The capacity gate's two negative controls already show C-W4 collapsing exactly to directed-W4 when the fault does not align with a compiled context, so the whole value of the abstraction rests on an empirical question nobody has answered: how often do real conditional faults align with a context a switch can compute in a few bits? (2026-08-29) | Three things, in order. (1) Expose `context_share` and `n_context` in `RunConfig`/`Fabric` and report collateral against share, including a share vector chosen ADVERSARIALLY against us, so the arithmetic component is visible rather than hidden. (2) Stop reporting a bare multiplier: report collateral reduction as a curve over share, with the arithmetic baseline drawn on the same axes. (3) Measure the real thing — `p(drop \| packet size, class, direction)` on physically degraded links with the SDE's pre/post-FEC counters as covariate. RS-FEC operates per codeword, so a 1500 B frame spans many more codewords than a 64 B frame and its loss probability is superlinearly higher at fixed codeword error rate; that is the mechanism behind Aegis's ">1KB only" incident and the curve is unpublished. That measurement supplies the real share vector, converts the abstraction from an index refinement into a model of a measured physical effect, and stands as a contribution even if every mechanism claim narrows. **Lesson: a ratio between two policies is a property of the workload split until you show it is not.** | evaluation | open, blocks the headline claim |
| H38 | **The notification channel saturates, and the grid's most expensive cell is past the cliff rather than on it.** Profiling `reorder_only` at p=1e-2 (94 s for 13 epochs, ~434 s for 60) showed the cost is not a bug: it is **857,034 `infer.update` calls in 13 epochs**, each sorting all 64 elements, because the witness genuinely emits ~100k events per epoch at that reorder rate. Turning that into a bound: the baseline witness emits 3 events per adjacent swap (H33), so event rate = packets x share x reorder_rate x sublinks x 3. Against the measured controller cost of 47 us per decision (one core, 21,277 events/s sustainable), one core **saturates at a reorder rate of 2.13e-4**, and the mirror channel carries 102 Mb/s of 128 B copies at 1e-3 and 1024 Mb/s at 1e-2. So the mechanism has an unstated **admission-control requirement**, and the p=1e-2 cell the frozen grid spends ~97 % of its 200 hours on is 47x beyond controller capacity and physically implausible besides (1 % of packets reordered). Cutting it is honest, not convenient — it is not an operating point, it is past the cliff (2026-08-29) | Report the notification bound as a first-class overhead row alongside stages and PHV, with the saturation rate derived rather than asserted. Adopt advance-only: it emits 2 events per swap instead of 3, which raises the saturation rate by exactly 3/2 to **3.19e-4** — so the fix is a 50 % capacity headroom gain on the control channel, not only a correctness fix, and that is the stronger argument for it. Add an explicit coalescing or admission rule at the collector and state it. Separately, report the hard scaling bound nobody has: `reg_wit_expect`/`reg_wit_observed`/`reg_wit_seq` are **1024 entries indexed by `md.sublink`**, so the abstraction tops out at 1024 (link x context) pairs — 64 vlinks x 16 contexts, or 256 vlinks x 4 — and `tbl_health_gate` at `size = 256` caps simultaneously quarantined sublinks. **Lesson: profile the expensive cell before buying CPU for it; the reason it is expensive is often the result.** | evaluation / overhead | open |
| H39 | **The hardware campaign cannot run as planned: there is no post-stamp fault injector, and the control plane would bind the wrong program.** Two blocking defects found by planning the campaign rather than by executing it. (a) `mcp_fabric_gate_event.p4` stamps the per-sublink sequence in EGRESS (`reg_wit_seq`, `wit_stamp`), but its only injector `tbl_fail` runs in INGRESS **after** `tbl_wit_check` and is gated off at `md.hop == LAST_HOP`. A packet is therefore counted by the witness and only then discarded, so the next arrival lands exactly where expected and **no gap is ever produced** — every ingress loss leaves the sequence contiguous. A gap requires loss strictly between the upstream egress deparser and the downstream ingress check, and no table occupies that window. The detector is structurally blind to the only fault we can inject, so C1 (end-to-end latency) and C3 (the headline lifecycle figure) cannot run at all. `mcp_fabric_w4_egdrop.p4` has a suitable `tbl_eg_fail` but is an older program with no capsule and no health gate. (b) `controller/hw_adapter.py:39` hardcodes `PROG = "mcp_fabric"` and binds it at lines 384-385, so against `mcp_fabric_gate_event` it resolves the wrong bfrt schema — which surfaces as wrong action arity or a silently empty table, not as a clean error. Neither `BfrtAdapter` nor `epoch_loop`'s bfrt path has ever executed against the switch (2026-08-29) | (a) Add an egress injector applied immediately after `tbl_wit_stamp`, keyed `md.sublink : exact` (making the fault exactly a behavioural-sublink fault, on the same index the witness uses, so injector and detector cannot disagree about the stratum) plus `hdr.witness.seq : range` (width 1 = a controller-armed deterministic one-shot for latency reps; width p*65536 = a periodic rate for the lifecycle figure), with a DirectCounter so "the injector fired N times" can be compared against "the witness saw N gaps" rather than assumed equal. Class 2 caps this at ONE 16-bit range field — a Bernoulli arm needs a second table with its own `Random`, not a second key. Compile locally on 9.13.1 first; it is free and catches placement before spending chip time. (b) Parameterise the program name. **Lesson: plan the campaign against the source, not against the intent. Both defects are invisible in a compile, in the PTF model, and in every simulation result we have.** | hardware campaign | open, BLOCKS C1 and C3 |
| H40 | **The capsule classifier and the C-W4 stratum table key on DIFFERENT quantities, so they bin the same packet differently near every boundary.** `tbl_context` (`mcp_fabric_gate_event.p4:909`) keys on `hdr.ipv4.total_len` — IP header plus payload, EXCLUDING the 14-byte Ethernet header and, on a looped pass, the 12-byte fabric shim, 14-byte CSIG tag and 4-byte witness. `tbl_stratum` in `mcp_fabric_cw4.p4:1079` keys on `eg_intr_md.pkt_length`, the whole frame as egress sees it. On a fabric pass C-W4's quantity is therefore `total_len + 44`. Using the frozen boundaries 256/1024/2048 verbatim on `total_len` puts a packet one stratum LOWER than C-W4 whenever `total_len` lands in `[B-44, B)` — 212..255, 980..1023, 2004..2047. Nothing currently published moves: every frozen scenario size (512, 1400, 1800, 4096 B) is at least 248 bytes from the nearest boundary, so offsets of 0, 14 and 44 all yield identical strata for every frozen row. But a capsule-vs-C-W4 comparison run near a boundary would show a difference that looks like a mechanism effect and is actually a keying effect (2026-08-29) | `plan_context()` implements the VERBATIM convention with `CTX_LEN_OFFSET = 0`, on the ground that the capsule stratum should be a property of the IP packet and therefore invariant across hops, shim depth and encapsulation — where `pkt_length` is not. The offset is a named constant so the alternative is one edit, and a unit test asserts the boundaries against `sim/sublink_capacity.SizeSchema` so the switch, the simulator and the P4 cannot drift apart silently. **What must NOT happen is comparing the capsule arm against the C-W4 arm at a boundary without stating this.** Any such comparison needs either a matched offset or an explicit note. Still to verify on silicon: whether `eg_intr_md.pkt_length` counts ingress-inserted headers, which sets the true offset to 44 rather than 30 | evaluation / dataplane | open, documented |
| H41 | **First contact with silicon found three defects that every compile and every PTF model run missed.** (a) The compiler-generated conf sets `model_json_path` to `share/<prog>/aug_model.json`, which bf-p4c emits only into an INSTALLED layout and which only the software model reads; bringup refused to boot on the missing file. The displaced sibling program's own working conf carries no such key at all. (b) **`sudo` strips `LD_LIBRARY_PATH` — and every `LD_*` — even with `-E`.** That is a sudo security behaviour, not a quoting bug, and it surfaced as `bf_switchd: error while loading shared libraries: libdriver.so`. Exporting before `sudo` is useless; it must be set on the sudo'd child via `env(1)`. (c) The bringup readiness probe waited 180 s for a log line matching `bfruntime gRPC server started on`, which **this SDE never emits** — `grep -c` over the whole switchd log returns 0 — while the server was in fact up and listening on 50052 the entire time. A readiness check keyed on a log string that the build does not print will always time out on a healthy system (2026-08-29) | (a) drop `model_json_path` when the file is absent, with a printed note, rather than refusing to boot -- hardware never reads it. (b) `sudo -E env LD_LIBRARY_PATH=... bf_switchd ...`, with a comment saying why, because the plain-export form looks more natural and will be "simplified" back by the next reader. (c) Use the LISTENER as the primary witness (`ss -ltn` on 50052) and treat any log line as corroboration only. **Lesson: three bugs, none of which a dry-run, a compile, or the PTF model could have caught, all found in the first ten minutes of contact with the real switch. This is the argument for bringing hardware forward in the schedule, not the argument against it.** | hardware campaign | (a) and (b) fixed, (c) open |
| H42 | **`Ingress.tbl_wit_verdict` fails to allocate at LOAD time although it compiles clean.** `pipe_mgr_exm_tbl_alloc: Error initializing table instance ... error No system resources`, followed by `pipe_mgr_tbl_add_init_default_entry: cannot get symmetric mode` and two failed assertions. It is an exact-match table with `size = 2`, one const entry (`16w0 : wit_ok()`), a const default action, and a DirectCounter `wit_ctr`. Exactly one table in the program fails; everything else initialises. The table is still visible to bfrt and `entry_get` succeeds returning 0 entries, which is normal for a const-entry table, so the failure is in the driver's table instance rather than in the schema. `tbl_wit_verdict` appears in ten programs, but the only program previously run on silicon (`mcp_fabric`) does NOT contain it — so this table has almost certainly never been loaded on hardware before and this is a first-contact defect, not a regression (2026-08-29) | UNRESOLVED, and it must not be worked around silently. Leading hypothesis: `size = 2` is below what the exact-match allocator can satisfy for a table carrying a direct counter, so the driver has no RAM unit to bind. Test it by raising `size` to a value the allocator can satisfy and reloading -- one hypothesis, one change. What is NOT yet known, and must be settled with traffic rather than reasoning: whether the DATA PLANE still classifies correctly, since the const entries live in the compiled binary and the failure is in pipe_mgr's shadow state. If the data plane is fine, the cost is losing `wit_ctr` as an on-chip ground-truth gap count, for which `tbl_eg_fail`'s DirectCounter and the gap events themselves are independent substitutes. Do not report any gap count sourced from `wit_ctr` until this is resolved | dataplane / hardware | open, BLOCKS ground-truth gap counting |
